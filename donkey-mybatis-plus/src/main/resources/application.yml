server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/donkey_study
    username: root
    password: lijinpeng123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml  # XML 配置文件路径
  type-aliases-package: com.donkey.common.entity  # 实体类包路径
  configuration:
#    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl #开启sql日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #开启sql日志


